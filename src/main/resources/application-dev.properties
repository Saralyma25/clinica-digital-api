# ==============================================================
# CONFIGURACAO DE AMBIENTE: DESENVOLVIMENTO (DEV)
# ==============================================================

# Mostra o SQL no console para debug (Raio-X do Banco)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Nivel de Log detalhado (Util para ver erros de query)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Garante que o Hibernate tente atualizar as tabelas localmente
spring.jpa.hibernate.ddl-auto=update

# Desativa caches para facilitar testes rapidos
spring.thymeleaf.cache=false